import { Box } from "@mui/material";
import { CollectionTable } from "./CollectionTable";

export const Collection = () => {
  return (
    <Box m="50px">
      <CollectionTable />
    </Box>
  );
};
